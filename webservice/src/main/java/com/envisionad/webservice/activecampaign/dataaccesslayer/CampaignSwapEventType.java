package com.envisionad.webservice.activecampaign.dataaccesslayer;

/** The actions retained in the active-campaign notification audit log. */
public enum CampaignSwapEventType {
    INITIAL_SELECTION,
    SWAP,
    MANUAL_NOTIFY,
    AUTO_NOTIFY
}
